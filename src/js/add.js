import { createApp } from "vue";
import Table from "../components/MatrixTable.vue"
import matrix from "./matrixFunctions.js";

console.log(matrix.addMatrix([[1, 2, 3]], [[1, 2, 3]]));

// createApp(Table).mount("#table1");
// createApp(Table).mount("#table2");

